{
  "name": "praman-setu-subgraph",
  "version": "1.0.0",
  "description": "Praman Setu IDAM indexer — identity, access, and digital asset events (PS 26125)",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:sepolia": "graph deploy --product hosted-service praman-setu/idam subgraph.yaml",
    "deploy:studio": "graph deploy praman-setu subgraph.yaml --version-label v1"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.81.0",
    "@graphprotocol/graph-ts": "^0.35.0"
  }
}
