{
  "name": "bel-chain-subgraph",
  "version": "1.0.0",
  "description": "BEL Chain IDAM indexer — identity, access, and digital asset events (PS 26125)",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:sepolia": "graph deploy --product hosted-service bel-chain/idam subgraph.yaml",
    "deploy:studio": "graph deploy bel-chain subgraph.yaml --version-label v1"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.81.0",
    "@graphprotocol/graph-ts": "^0.35.0"
  }
}
